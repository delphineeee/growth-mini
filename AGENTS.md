# Growth Mini — Coding Agent Handoff

> 状态快照：2026-08-20（用户本地时间）  
> 仓库：`delphineeee/growth-mini`  
> 当前基线：`main`，PR #1 已合并，版本 `0.1.0`

本文档是本仓库的项目级交接与实现约束。编码 Agent 在规划或修改代码前，应先阅读本文档、`README.md`、相关实现与测试；如代码、测试、文档互相矛盾，以可验证的当前代码和用户最新明确决定为准，并同步修正文档。

## 1. 一句话定义

Growth Mini 是一个独立、证据驱动的职业成长小产品：从用户人工收集的真实 JD 中提取岗位能力要求，对照用户已有的可验证作品，排序最值得补足的证据缺口，并生成一个有明确验收标准的 7 天作品冲刺。

它不是录用概率预测器，不是人格或能力测评，也不是泛化的“AI 人生规划师”。

## 2. 项目背景与目标

### 2.1 起源

Growth Mini 来源于原项目 Growth AI Studio 的一次收缩与重构。原项目希望根据行业、岗位和个人情况生成学习路径，但始终存在一个核心可信度问题：

- AI 推荐的课程是否真的有效，无法验证；
- AI 生成的长期路径是否客观，无法证明；
- 产品范围过大，难以在短时间内形成可运行、可测试、可汇报的闭环；
- “未来十年风口行业”等开放式判断会扩大不确定性，并偏离用户真正要解决的问题。

因此，当前决定不是继续大改 Growth AI Studio，而是保留原仓库与思路，另建独立仓库 Growth Mini，用更窄的目标验证一条可信闭环。可以借鉴原项目代码和产品经验，但不要让本项目重新膨胀成原来的大产品。

### 2.2 产品目标

本阶段只验证以下命题：

> 基于一组真实岗位要求和用户已有作品证据，能否帮助用户在较短时间内选出一个合理的补强方向，并最终做出一份别人可以检查的作品证据？

成功不等于“推荐得很智能”，而等于：

1. 每条岗位要求能够追溯到当前数据集中的来源文件和短摘录；
2. 用户能够理解排序公式，而不是相信黑箱结论；
3. 用户能在 10 分钟左右完成证据登记并选定一个目标；
4. 7 天任务有明确产物和逐日验收条件；
5. 结束后能记录完成率、实际耗时、预测偏差和产物链接；
6. 最终形成 GitHub 仓库、Demo、测试报告、PR 或完整技术文档等可检查成果。

## 3. 目标用户与真实需求

### 3.1 当前首要用户

第一位用户就是项目创建者本人：电子信息工程背景、希望转向 AI / Agent 相关岗位的大学生。当前不是为成熟工程师或大规模招聘团队设计。

### 3.2 用户真正需要的能力

- 从开放、模糊的学习方向中得到一个足够小的下一步；
- 获得外部结构和轻量推动，而不是一份宏大的长期规划；
- 推荐理由必须有证据、可解释、可复核；
- 每项任务必须能验收，避免“学了很多但没有作品”；
- 尽快获得可运行结果和正反馈，不要求先完整学完机器学习课程；
- 用户可以承认“暂无证据”，产品不得用羞辱性或价值判断语言；
- 产品应减少前置整理工作，不能要求用户为了启动 MVP 先收集 100 份 JD 或补齐所有旧链接。

### 3.3 当前非目标用户

- 招聘方、HR 或企业人才评估部门；
- 需要严肃录用预测、薪酬预测或就业市场统计的人；
- 期待完整课程平台、知识库或长期人生规划的人；
- 需要账户、多设备同步、团队协作或商业级数据治理的人。

## 4. 已经做出的产品决定

这些决定是当前实现边界。除非有新的用户证据，不要自行推翻。

### 4.1 独立项目，而非大改 Growth AI Studio

- Growth Mini 作为独立仓库和独立小产品演进；
- Growth AI Studio 暂不大改，也不要求同步功能；
- 可以复用原项目的思路、视觉经验或局部代码，但不要建立强耦合。

### 4.2 先做小而完整的证据闭环

固定主链路：

1. 读取并筛选 JD；
2. 生成岗位能力证据矩阵；
3. 用户登记个人证据；
4. 计算证据缺口优先级；
5. 用户选择一个能力；
6. 生成 7 天作品冲刺；
7. 记录实际完成情况；
8. 生成结项证据报告。

任何新增功能都应说明它改善了上述哪一步，以及如何测量；否则默认不做。

### 4.3 确定性规则优先于 LLM 黑箱

MVP 使用关键词规则、明确权重和固定模板，而不是让模型直接判断“你应该学什么”。理由是：

- 输出可重复；
- 便于测试和解释；
- 不需要 API Key；
- 不会把模型语言流畅度误当作证据；
- 更容易发现分类和权重错误。

未来可以用 LLM 辅助归一化、候选技能提取或解释，但必须保留来源摘录、确定性回退和评测集，不得让 LLM 成为无法审计的唯一判断者。

### 4.4 评分只表示“补强优先级”

当前公式：

```text
priority = role_weight × evidence_gap × 100
```

岗位要求权重：

- `required`：`1.0`
- `responsibility`：`0.8`
- `preferred`：`0.6`

个人证据缺口：

- `verified`：`0.0`
- `claimed`：`0.55`
- `none`：`1.0`

不得把该分数描述为：

- 能力百分比；
- 岗位匹配率；
- 录用概率；
- 个人价值评分；
- 对未来表现的预测。

修改权重前必须补充理由、测试，并优先从人工标注或用户研究获得依据。

### 4.5 证据等级采用三态

- `verified`：有别人可以检查的作品，如仓库、在线 Demo、测试报告、公开 PR 或完整技术文档；
- `claimed`：学过、做过、记过笔记，但当前缺少外部可检查成果；
- `none`：暂无相关证据。

课程完成、阅读和自述不能自动升级为 `verified`。链接字段当前可选，但报告要诚实区分完成任务与形成外部证据。

### 4.6 7 天而非年度计划

当前只生成 7 天冲刺。每天包含：

- 主题；
- 具体任务；
- 可检查的验收标准；
- 预计分钟数；
- 完成状态；
- 实际分钟数；
- 当日产物链接。

冲刺最终应产生一个小型技术作品，而不是只产生学习清单。

### 4.7 个人数据暂存在浏览器

- 个人证据和签到记录保存在 `localStorage`；
- 当前不做登录、服务端用户数据库或云同步；
- 不上传个人记录是当前的隐私优点，也是多设备不同步的限制；
- 在完成 3–5 人试用前，不要优先建设账户系统。

## 5. 被否决或暂缓的方案

### 5.1 继续大改 Growth AI Studio

否决原因：范围过大、历史包袱重、验证问题没有被解决。Growth Mini 应保持独立。

### 5.2 生成长期人生规划、年度计划或“未来十年风口”

否决原因：结论难验证、极易过时、容易制造虚假权威感，也无法快速形成作品。

### 5.3 直接由 AI 推荐课程和完整学习路径

否决原因：课程有效性、排序依据和适配性缺乏证据。课程推荐不是当前 MVP 的核心。

### 5.4 先收集 100 份 JD 再开发

否决原因：前置工作量过大，容易把数据收集当成进展，延迟真实用户验证。当前 11 份足以测试产品闭环，但不足以支持市场级结论。

### 5.5 重新寻找全部历史 JD 的原始链接

否决原因：用户已经通过多个官方招聘网页人工挑选和收集，但没有保存链接；事后回溯成本高。旧链接不要求补录，也不得猜测或伪造。

### 5.6 宣称数据具有外部可复核性

否决原因：历史链接未保留。当前数据只能诚实描述为“公司官网人工收集的内部验证样本，链接未保留”。

### 5.7 用来源不明的百分比包装可信度

否决原因：会制造伪精确。所有数字必须能由当前数据和公开公式计算出来。

### 5.8 在验证前增加认证、复杂数据库、支付、社交或内容平台

暂缓原因：不改善当前核心命题，会增加部署和维护成本。

## 6. 数据来源与数据约束

### 6.1 当前数据集

目录：`data/raw_jds/`

当前共 11 份 JD：

- `application`：6 份；
- `algorithm`：4 份；
- `product`：1 份。

当前规则能识别 25 个技能条目。数量是当前快照，不应硬编码到产品逻辑；测试中的数量断言用于提醒数据集发生变化。

### 6.2 来源性质

- 用户在 2026 年 8 月打开多个公司官方招聘页面，人工检索、挑选并保存岗位内容；
- 历史原始 URL 未保留，不要求用户重新寻找；
- 收集日期在应用层统一为 `YYYY-MM`，当前均为 `2026-08`；
- `jd01`–`jd07` 主要保留用户收集的原始内容；
- `jd08`–`jd11` 是从用户提供材料整理出的结构化摘要；
- 不得在 README、Issue、PR 或 Agent 回复中大段复制第三方 JD 原文；引用时使用短摘录或结构化概括。

### 6.3 来源证据最低字段

每份新 JD 应尽量包含：

```markdown
- 公司：
- 岗位名称：
- 原始链接：
- 收集日期：YYYY-MM
- 来源类型：
- 工作地点：
- 实习或校招：
```

对未来新增 JD：如果当时能获得 URL，应保存 URL；不要因为旧数据缺失链接而继续故意丢失新链接。

对旧数据：空链接在界面中应显示“链接未保留”，不得生成貌似真实的 URL。

### 6.4 文件兼容性
- 不要在没有迁移和测试的情况下把加载逻辑改回 glob `*.md`；
- 解析器同时兼容 `## JD 原文` 和 `## 结构化要求`。

## 7. 当前实现

### 7.1 技术栈

- Python 3.12；
- FastAPI；
- Pydantic；
- Uvicorn；
- 原生 HTML / CSS / JavaScript；
- Pytest + HTTPX；
- Docker；
- GitHub Actions。

当前没有前端构建步骤、Node 依赖、数据库或外部模型 API。

### 7.2 目录职责

```text
app/main.py                  FastAPI 路由、静态文件挂载
app/models.py                请求模型与枚举约束
app/services/jd_parser.py    JD 解析、岗位分类、技能矩阵、来源证据
app/services/scoring.py      透明优先级公式
app/services/sprint.py       7 天冲刺模板与结项报告
data/raw_jds/                11 份岗位样本
static/index.html            单页界面结构
static/styles.css            响应式视觉样式
static/app.js                状态、API 调用、localStorage 与交互
tests/                       解析、评分、冲刺、API 测试
.github/workflows/test.yml   push / PR 自动测试
Dockerfile                   容器启动配置
```

### 7.3 API

- `GET /api/health`：健康检查与版本；
- `GET /api/dataset?role_family=...`：数据集摘要；
- `GET /api/matrix?role_family=...`：能力矩阵与来源；
- `POST /api/priorities`：根据个人证据排序；
- `POST /api/sprint`：生成 7 天冲刺；
- `POST /api/report`：汇总完成率、实际耗时、偏差和证据等级；
- `GET /`：返回前端页面。

`role_family` 仅允许：

```text
all | application | algorithm | product
```

### 7.4 JD 解析

`SKILL_TAXONOMY` 是显式技能词典，每项包含展示名称、类别和正则模式。

矩阵按每份 JD 去重：同一技能在同一 JD 中最多计一次，并保留该 JD 中优先级最高的要求类型：

```text
required > responsibility > preferred
```

每条来源保留：

- JD ID；
- 公司；
- 岗位名称；
- 要求类型；
- 最多 140 字的摘录；
- 仓库文件路径；
- 原始 URL；
- 来源类型。

### 7.5 前端状态

前端是单页、无框架实现。主要状态存于 `state`：

- 当前岗位类别；
- 能力矩阵；
- 排序结果；
- 当前冲刺；
- 个人证据；
- 签到记录。

以下内容保存到浏览器：

```text
growth_mini_evidence
growth_mini_checkins
```

切换浏览器、无痕窗口、设备或清理网站数据会丢失这些记录。

### 7.6 当前测试状态

截至本快照：

- 9 项测试通过；
- GitHub Actions 最新完整构建成功；
- 覆盖数据数量、月份标准化、来源存在性、产品岗位筛选、评分顺序、7 天冲刺、报告计算、主 API 链路和无效输入；
- 尚无浏览器端自动化测试和视觉回归测试。

## 8. 本地开发与运行

### 8.1 Windows PowerShell 推荐命令

首次下载公开仓库不必先登录 GitHub CLI：

```powershell
git clone https://github.com/delphineeee/growth-mini.git
cd .\growth-mini
py -3 -m venv .venv
& .\.venv\Scripts\python.exe -m pip install -r .\requirements.txt
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

打开 `http://127.0.0.1:8000`。该地址只有在本机 Uvicorn 正在运行时才能访问，不能把 README 中的 localhost 链接当作已部署公网网站。

以后启动：

```powershell
cd D:\Code\growth-mini
& .\.venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

### 8.2 测试

```powershell
& .\.venv\Scripts\python.exe -m pytest -q
```

或在已激活的 Unix 虚拟环境中：

```bash
pytest -q
node --check static/app.js
python -m compileall -q app
```

## 9. 用户与编码 Agent 的职责边界

### 9.1 用户负责

- 决定首轮重点岗位类别；
- 自己填写真实的个人证据，不编造项目或经历；
- 完成 7 天冲刺并记录实际耗时、完成情况和产物；
- 邀请 3–5 位目标用户试用并收集反馈；
- 决定部署平台、公开范围和是否对外展示；
- 对今后新增 JD 尽量保存原始 URL 和收集月份。

用户不需要：

- 重新寻找 11 份历史 JD 的链接；
- 在产品验证前收集 100 份 JD；
- 先学完整套机器学习课程才能继续开发；
- 手工计算矩阵或评分。

### 9.2 编码 Agent 负责

- 维护解析、评分、冲刺和报告主链路；
- 为每项行为变化补充测试；
- 保持输出可解释、可追溯；
- 改善本地启动、错误提示、试用记录和部署体验；
- 从试用数据中总结产品问题，而不是替用户编造结论；
- 对不确定的数据来源明确标注限制；
- 在修改公式、分类或数据结构时提供迁移说明。

编码 Agent 不得：

- 编造 JD 链接、公司要求、用户项目、使用数据或反馈；
- 把短样本统计包装成就业市场普遍规律；
- 未经证据把确定性排序替换为 LLM 黑箱建议；
- 擅自把项目扩展为综合职业规划平台；
- 为追求“完整”要求用户补做高成本、低价值的数据工作；
- 静默删除或覆盖用户收集的原始 JD；
- 在文档、Issue 或回复中大段转载第三方 JD 原文。

## 10. 已知问题与技术债

### 10.1 尚未部署公网版本

Dockerfile 已存在，但没有确认中的线上环境或固定公网 URL。localhost 不是部署结果。部署前应明确平台、隐私范围和最小日志策略。

### 10.2 数据规模与代表性有限

- 仅 11 份 JD；
- 公司和岗位方向分布不均；
- 产品岗位只有 1 份；
- 统计只能代表当前样本；
- 历史 URL 缺失，无法让第三方直接回到原页面核验。

### 10.3 抽取规则尚未做人工评测

- `SKILL_TAXONOMY` 可能漏召回同义词；
- 一个宽泛词可能误匹配；
- `_role_family()` 是标题和正文启发式规则；
- `_requirement_type()` 依据行内词语和“加分项”段落状态判断，可能误分类；
- 尚无人工金标、precision、recall 或混淆分析。

### 10.4 前端只展示前 14 项个人证据输入

`renderEvidence()` 使用 `state.matrix.slice(0,14)`。其余技能虽在矩阵和排序后端中存在，但用户不能直接在当前表单中为全部技能登记证据。这是产品取舍还是遗漏尚未经过验证。

### 10.5 浏览器状态存在局限

- 无多设备同步；
- 无导入导出；
- 清理 localStorage 会丢失数据；
- 重新生成冲刺会清空签到；
- 没有版本迁移机制；
- 没有显式“清空我的数据”按钮。

### 10.6 表单与报告校验有限

- 作品 URL 和产物 URL 当前是普通字符串，没有验证格式；
- `HttpUrl` 被导入但未使用；
- 报告的 7 天分母是固定值；
- 完成 6 天且至少一个链接即判为 `verified`，标准可能过于粗糙；
- 没有阻止把无效或虚构链接当作作品证据。

### 10.7 可访问性和浏览器测试不足

- 没有 Playwright/Cypress 等端到端测试；
- 没有键盘导航、屏幕阅读器和颜色对比专项验收；
- 已做响应式 CSS，但尚无系统化的多浏览器、多尺寸视觉验证。

### 10.8 错误与加载状态较弱

- 初始矩阵加载有错误文字；
- 其他 POST 操作缺少统一错误处理、重试、禁用按钮和加载反馈；
- 网络异常时用户可能不知道数据是否已经保存。

### 10.9 前端源码可维护性

- `static/app.js` 和 `static/styles.css` 当前高度压缩，很多逻辑集中在单行；
- 小改动尚可，但继续扩展会增加审查、冲突和调试成本；
- 在增加较复杂交互前，应先用不改变行为的格式化提交改善可读性，并运行前端语法检查和主链路回归；
- 暂无必要因此引入 React、Vue 或构建工具，除非原生实现已经成为经过验证的瓶颈。

## 11. 未完成事项

### P0：让用户真正跑起来并完成一次闭环

1. 修订 README，提供独立的 Windows PowerShell 快速开始；
2. 可选增加 `scripts/start_windows.ps1`，自动检查 Python、创建虚拟环境、安装依赖并启动；
3. 为常见错误提供明确提示：仓库未下载、Python 不存在、端口占用、依赖未安装；
4. 让创建者本人完整走一次“证据 → 排序 → 冲刺 → 报告”；
5. 记录首次使用中断点，不先猜测用户会在哪里失败。

### P1：建立产品有效性的最小证据

1. 邀请 3–5 位 AI / Agent 求职或学习方向相近的学生试用；
2. 记录匿名、最小化指标：
   - 是否完成证据填写；
   - 是否理解排序理由；
   - 是否选出目标能力；
   - 是否生成冲刺；
   - 第 1、3、7 天完成情况；
   - 预计与实际耗时；
   - 是否产生可检查作品；
3. 使用简短访谈而非只看点击量：哪里不可信、哪里太重、哪里没有帮助；
4. 在没有同意前不收集敏感个人信息。

### P1：评测 JD 抽取

1. 为 11 份 JD 制作独立人工标注表；
2. 标注技能、要求类型和岗位类别；
3. 计算技能提取 precision / recall；
4. 记录误报、漏报和同义词；
5. 基于错误修订 taxonomy，而不是为了提高数字随意改变标注。

### P2：提高证据与结果的可携带性

1. 增加 JSON 导出/导入；
2. 生成可复制或下载的结项报告；
3. 为个人证据增加更新时间和备注；
4. 在不引入账户系统的情况下支持本地备份；
5. 明确“任务完成”与“作品已验证”的区别。

### P2：部署可访问的试用版本

1. 选择一个支持 Docker 或 Python Web 服务的平台；
2. 配置健康检查 `/api/health`；
3. 记录部署步骤和回滚方法；
4. 不在日志中记录个人证据内容；
5. 部署后重新完成移动端和桌面端主链路测试。

### P3：只在证据支持时扩展

可选方向：

- 增加更多岗位类别和更均衡的样本；
- LLM 辅助技能归一化，但保留规则回退与评测；
- 基于真实冲刺结果校准时间估算；
- 允许用户自定义 7 天目标和验收条件；
- 账户和云同步，仅在多设备需求被验证后考虑。

## 12. 下一步优先级与建议执行顺序

下一位编码 Agent 默认按以下顺序工作，不要直接跳到大型新功能：

1. **本地启动修复**：更新 Windows README，必要时增加启动脚本；
2. **本机验收**：让用户看到页面并完成一次主链路；
3. **修复真实阻塞**：只处理首次使用暴露的高影响问题；
4. **建立抽取金标**：评估当前 25 项技能规则的准确性；
5. **3–5 人试用**：收集闭环完成率和理解度；
6. **报告导出**：让成果可以离开当前浏览器；
7. **部署试用版**：得到稳定公网入口；
8. **根据证据决定扩展或收缩**。

如果没有新的用户反馈，默认第一项工程任务是：

> 完善 Windows 本地启动体验，并保证一位不熟悉 Python 环境管理的用户能够从空目录开始，在 10 分钟内打开 Growth Mini。

## 13. 关键对话结论

以下结论应被视为产品历史中的关键判断：

1. 原产品最难的问题不是页面或模型能力，而是推荐结论缺乏可验证依据；
2. 用户需要的是“下一份证据”，不是对未来的宏大预测；
3. 小产品必须形成闭环，不能只完成 JD 分析而没有行动与回访；
4. 7 天计划优于年度计划，因为更容易启动、验收和复盘；
5. 岗位 JD 需要用户亲自从真实页面挑选；解析、统计、评分和生成冲刺由程序完成；
6. 当前没有必要继续收集大量 JD，先用 11 份验证使用价值；
7. 历史 URL 丢失是数据限制，不是要求用户补作业的理由；
8. 可信度来自公开公式、来源路径、短摘录、测试和诚实限制，不来自 AI 的肯定语气；
9. 用户的低启动成本是产品约束：安装、运行和填写步骤都必须尽量短；
10. GitHub 负责代码同步，云端 Agent 与本地 Agent 的文件权限和工具安装状态互不等价。

## 14. 编码与交付规则

### 14.1 修改前

1. 阅读本文档和相关代码；
2. 运行 `git status -sb`，保护用户已有修改；
3. 从最新 `main` 创建 `agent/<description>` 分支；
4. 先写清改动改善了哪一段产品闭环；
5. 对数据或算法改动，说明证据来源和预期影响。

### 14.2 修改中

- 保持实现小而可测试；
- 优先修改已有模块，不创建无必要的新抽象；
- 不引入外部服务，除非它直接解决已验证问题；
- API 行为变化必须更新 Pydantic 模型、前端调用和测试；
- 数据格式变化必须兼容 `jd01_md` 或提供明确迁移；
- 所有用户可见数字必须能从数据或公式推导；
- 错误信息应使用清晰中文，避免技术栈泄漏和责任归咎语言。

### 14.3 最低验证

```bash
pytest -q
node --check static/app.js
python -m compileall -q app
```

涉及界面时，还应实际启动服务并走通：

```text
选择岗位 → 展开 JD 来源 → 填写证据 → 计算优先级
→ 生成冲刺 → 填写签到 → 生成报告
```

### 14.4 提交与 PR

- 不直接向 `main` 写入未经审阅的功能；
- 一个逻辑改动尽量一个正常提交；
- 避免逐文件远端提交触发大量中间 CI；
- PR 描述必须包含改动、原因、用户影响、验证结果和残余风险；
- CI 失败时先看完整日志，不要凭邮件标题判断最终版本失败；
- 不合并测试失败或数据声明不准确的改动。

## 15. 完成定义

一个功能只有同时满足以下条件才算完成：

- 改善了明确的用户问题；
- 主链路仍能运行；
- 有与风险相称的自动化测试；
- 用户可见文案不夸大数据或算法能力；
- 新增数据有来源状态和收集月份；
- Windows 用户能够按文档复现；
- README / AGENTS.md 与实现保持一致；
- 已说明尚未解决的限制。

不要把“代码已生成”当作完成，也不要把“AI 给出了合理解释”当作验证。
